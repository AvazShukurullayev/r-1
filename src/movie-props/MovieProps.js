import React from 'react';
import {MovieCards} from "./components";

const MovieProps = () => {
    return (
        <div className="movie__props">
            <MovieCards />
        </div>
    );
};

export default MovieProps;