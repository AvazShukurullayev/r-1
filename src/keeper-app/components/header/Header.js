import React from 'react';

const Header = () => {
    return (
        <header className="header bg-warning p-3 shadow mb-5">
            <div className="container">
                <h1 className="font-monospace text-light">Keeper App</h1>
            </div>
        </header>
    );
};

export default Header;